# -*- coding: utf-8 -*-
"""Внутренние модули ядра экспорта IFC.

Назначение:
    - Содержит оркестратор, доменные модели, работу с <HISTORY_NAME>.xlsx,
      подготовку задач и интеграцию с pyRevit.
    - Инкапсулирует бизнес-логику поверх конфигурации и утилит.

Контракты:
    - Пакет не предоставляет «магический» публичный API: необходимые
      компоненты импортируются из конкретных модулей
      (exporter, manage, history, ifc_checker, tasks и т.п.).
    - Импорт core.* не должен порождать побочных эффектов: запуск
      пайплайна экспорта выполняется только через main.py и
      ExportOrchestrator.run().
    - Все пути и имена файлов берутся из config.settings/config.files/
      config.constants, без хардкодов внутри ядра.

Особенности:
    - Модули пакета рассчитаны на исполнение под CPython 3.x.
    - Допускается использование современного синтаксиса typing
      (Path | str, list[str] и т.п.), так как код ядра не выполняется
      под IronPython.
    - Весь код, напрямую работающий с Revit API, вынесен в пакет
      revit и вызывается через pyRevit-скрипты.
"""
__version__ = "2.0.0"

# -*- coding: utf-8 -*-
"""Пакет с кодом, выполняющимся внутри Revit/pyRevit.

Назначение:
    - Содержит модули, которые работают в процессе Revit через pyRevit
      и IronPython 3.4.
    - Предоставляет инфраструктуру для:
        * чтения задач выгрузки из admin_data/<TMP_NAME>.csv;
        * описания ExportJob и связанных структур;
        * поиска 3D-видов для экспорта;
        * настройки IFCExportOptions и выполнения экспорта IFC.

Контракты:
    - Пакет не должен содержать тяжёлой логики на уровне импорта:
      при подключении модуля revit.* не инициируются побочные эффекты.
    - Весь код, взаимодействующий с Revit API, находится в отдельных
      модулях (jobs, task_reader, views, ifc_options, _api и т.п.) и
      вызывается из pyRevit-скриптов.

Особенности:
    - Модули пакета могут выполняться под IronPython 3.4 (pyRevit).
    - Допускается использование type hints и модуля typing, но без
      синтаксиса, требующего более новых версий Python
      (list[str], X | Y и т.п.) в исполняемом коде.
"""

# -*- coding: utf-8 -*-
"""Вспомогательные утилиты проекта ExportIFC.

Назначение:
    - Общие функции и классы для работы с файловой системой, путями,
      Excel, логами, версиями Revit и вспомогательными преобразованиями.
    - Используются как внутренняя библиотека в модулях core и config,
      а также частично из pyRevit-слоя.

Контракты:
    - Пакет не должен зависеть от Revit API и pyRevit; все зависимости
      на Revit-инфраструктуру сосредоточены в пакете revit.
    - Импорт utils.* не создаёт побочных эффектов: любая работа выполняется
      только через явный вызов функций и методов.
    - Все «магические» значения (имена файлов, листов, колонок и т.п.)
      должны приходить из config.*, а не задаваться внутри утилит.

Особенности:
    - Основной сценарий выполнения — CPython 3.x с использованием
      современного typing.
    - Отдельные модули пакета могут импортироваться из кода, выполняемого
      под IronPython (логирование и совместимость), поэтому в них
      избегается синтаксис, недоступный в IronPython (PEP 604 и т.п.).
"""
